language: node_js
node_js:
  - '24'
cache:
  directories:
    - ~/.pnpm-store
install:
  - corepack enable
  - pnpm install --frozen-lockfile
  - npx playwright install --with-deps chromium
script:
  - pnpm lint
  - pnpm build
  - pnpm test
